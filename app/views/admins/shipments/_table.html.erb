<table class="table c-table">
  <thead>
    <tr>
      <th>支払い日</th>
      <th>注文番号</th>
      <th>ステイタス</th>
      <th>支払い方法</th>
      <th>名前</th>
      <th>合計金額</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% shipments and shipments.each do |shipment| %>
      <tr>
        <td><%= shipment.payment.created_at.strftime("%Y/%m/%d %H:%M") %></td>
        <td><%= shipment.payment.number %></td>
        <td><%= shipment.state %></td>
        <td><%= shipment.payment.payment_method.name %></td>
        <td><%= shipment.payment.user.name %></td>
        <td><%= shipment.payment.amount %>円</td>
        <td>
          <a href="<%= admins_shipment_path(shipment) %>" class="btn btn-success btn-sm"><i class="fa fa-edit"></i></a>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
