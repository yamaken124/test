<%= form_for([:admins, variant.product, variant]) do |v| %>
  <section class="content">
  <div class="box-body col-md-6">
    <%= v.hidden_field :sku, :value => "all" %>
      <div class="form-group">
        <p><%= v.label :name %></p>
        <%= v.text_field :name, placeholder: variant.product.name, class: "form-control" %>
      </div>
      <div class="form-group">
        <p><%= v.label :is_valid_at %></p>
        <%if params[:action]=="new" %>
          <%= v.date_field :is_valid_at, value: Date.today%><br/>
        <% else %>
          <%= v.date_field :is_valid_at %><br/>
        <% end %>
      </div>
      <div class="form-group">
        <p><%= v.label :is_invalid_at %></p>
        <%if params[:action]=="new" %>
          <p><%= v.date_field :is_invalid_at, value: Date.today+6*30 %></p>
        <% else %>
          <p><%= v.date_field :is_invalid_at %></p>
        <% end %>
      </div>
      <div class="form-group">
        <p>
          <%= v.select :order_type,{t('order_type.single_order') => "single_order", t('order_type.subscription_order') => "subscription_order"},{},{:class=>"form-control"} %>
        </p>
      </div>
      <%= v.fields_for :price do |p| %>
        <div class="form-group">
          <p><%= p.label :amount %></p>
          <p><%= p.text_field :amount, class: "form-control", value: variant.price.amount %></p>
        </div>
      <% end %>
      <div class="actions">
        <%= v.submit "登録", class: "btn" %>
      </div>
    </div>
    </section>
<% end %>
