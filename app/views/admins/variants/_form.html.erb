<%= form_for([:admins, variant.product, variant]) do |v| %>

  <div class="field">
    <%= v.label :sku %><br>
    <%= v.text_field :sku, class: "text" %>
  </div>
  <div class="field">
    <%= v.label :is_valid_at %><br>
    <%= v.date_field :is_valid_at %><br/>
  </div>
  <div class="field">
    <%= v.label :is_invalid_at %><br>
    <%= v.date_field :is_invalid_at %><br/>
  </div>
  <div class="field">
    <%= v.select :order_type, {t('attributes.order_type.single_order') => "single_order", t('attributes.order_type.subscription_order') => "subscription_order"} %>
  </div>
  <%= v.fields_for :prices do |p| %>
    <div class="field">
      <%= p.label :amount %><br>
      <%= p.text_field :amount, class: "text", value: variant.prices.pluck(:amount) %>
    </div>
  <% end %>
  <div class="actions">
    <%= v.submit "登録", class: "submit" %>
  </div>

<% end %>